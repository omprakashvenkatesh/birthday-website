{
  "name": "birthday-website",
  "homepage": "https://omprakashvenkatesh.github.io/birthday-website/",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A birthday film in four acts: draw a Cupid bow, strike a heart, and watch it bloom into a tree of hearts. Vanilla JS + GSAP, one canvas, no framework.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
      "predeploy": "npm.cmd run build",
  "deploy": "gh-pages -d dist"

  },
  "dependencies": {
    "gh-pages": "^6.3.0",
    "gsap": "^3.13.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
